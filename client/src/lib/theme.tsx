import { extendTheme } from "@chakra-ui/react";

export const customTheme = extendTheme({
  colors: {
    background: "#8D775F",
    filters: "green",
    cardHeader: "cyan",
    postitbg: "#E1F0C4",
    tag: "#222E50",
    inputHighlight: "#BC8DA0",
  },
});
